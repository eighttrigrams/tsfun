import {split} from '../../src/array'


/**
 * tsfun | split
 */
describe('split', () => {

    it('split', () =>
        expect(

            split('')('abc')

        ).toEqual(['a', 'b', 'c']))
})
