import {isDefined} from '../../src/predicate'


/**
 * tsfun | isDefined
 */
describe('isDefined', () => {

    it('isDefined',() =>
        expect(

            isDefined(true))

            .toEqual(true))
})
